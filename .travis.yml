# Use Dockerized infrastructure
sudo: false
language: node_js
node_js: 
  - "stable"

# Cache our Gcloud SDK between commands
cache:
  directories:
  - "$HOME/google-cloud-sdk/"
  - "$HOME/.npm/"

branches:
  only:
    - master
    - travis_impl
    - feature_docker
#   - develop
#env:
# Make sure gcloud command is on our PATH and the App Engine SDK is in the Python path
#- GAE_PYTHONPATH=${HOME}/.cache/google_appengine PATH=$PATH:${HOME}/google-cloud-sdk/bin PYTHONPATH=${PYTHONPATH}:${GAE_PYTHONPATH} CLOUDSDK_CORE_DISABLE_PROMPTS=1
before_install:
# Install Google App Engine Python SDK
#- if [ ! -d "${GAE_PYTHONPATH}" ]; then
#    python scripts/fetch_gae_sdk.py $(dirname "${GAE_PYTHONPATH}");
#  fi
# [START auth]
# Decrypt the credentials we added to the repo using the key we added with the Travis command line tool
- openssl aes-256-cbc -K $encrypted_0981bbf19127_key -iv $encrypted_0981bbf19127_iv -in credentials.tar.gz.enc -out credentials.tar.gz -d
# If the SDK is not already cached, download it and unpack it
- if [ ! -d ${HOME}/google-cloud-sdk ]; then
     curl https://sdk.cloud.google.com | bash;
  fi
- tar -xzf credentials.tar.gz
- mkdir -p lib
# Here we use the decrypted service account credentials to authenticate the command line tool
#- gcloud components install docker-credential-gcloud
- gcloud auth activate-service-account --key-file client-secret.json
- gcloud auth configure-docker
- expect "Do you want to continue (Y/n)?"
- send "Y\r"

# [END auth]
#before_install:
- cd frontend/todo-app
- npm ci
- cd ../../restful-web-services
#- mvn compile THIS
install:
# Set the correct project to deploy to
- gcloud config set project sept-248413
# Install the dependencies
script:
# Run the unit tests
#- cd ../frontend/todo-app THIS
#- npm test THIS
#- cd ../../restful-web-services THIS
#- mvn test THIS
# [START deploy]
# Deploy the app
- mvn -Dmaven.test.skip=true package
- docker build -t gcr.io/sept-248413/market-app:latest .
- docker push gcr.io/sept-248413/market-app:latest
- cd ../frontend/todo-app
- docker build -t gcr.io/sept-248413/market-front:latest .
- docker push gcr.io/sept-248413/market-front:latest
#- gcloud -q app deploy app.yaml --promote
# Run and end to end test

# [END deploy]